/* Write a program that:
Takes an integer n (size of array).
Takes n elements to create the array.
Then takes one more element to insert at the beginning of the array (without using .unshift()).
Print the updated array after insertion. */
