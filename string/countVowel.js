/* Question: Write a function that returns the number of vowels in a string.
// Input: "hello world"
// Output: 3
 */

function countVowel(word){
    
    for(let i=0; i<word.length; i+=1){
        if (arr[i]=="a" || arr[i]=="a" || arr[i]=="" || arr[i]=="o" || arr[i]=="u"){}
    }

}